# Sample Entity Config
# Generated by /add-entity-config workflow
# Update this file as needed

entity:
  name: sample
  display_name: Sample Entity
  status: pending  # pending | ready | finalize

source:
  sql: |
    SELECT *
    FROM SAMPLE_TABLE
    WHERE ROWNUM <= :limit

target:
  object: Sample_Object__c
  operation: upsert
  external_id_field: External_Id__c

batch:
  size: 40000
  parallel: true

# Reference to mapping file
mapping_file: configs/mappings/sample.yaml

# Optional GX suite name
gx_suite: null
