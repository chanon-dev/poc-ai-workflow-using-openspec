# Sample Mapping Config
# Generated by /add-mapping workflow
# Source: center_docs/...datadic.md

entity: sample

source:
  tables:
    - SAMPLE_TABLE
  primary_key:
    - ID

target:
  object: Sample_Object__c
  operation: upsert
  external_id_field: External_Id__c

# KP Field (Oracle) -> SFDC Field (Salesforce)
mapping:
  ID: External_Id__c
  NAME: Name
  CREATED_DATE: CreatedDate__c

# Type transformations
transformations:
  datetime:
    - CREATED_DATE
  decimal: []
  boolean: []
